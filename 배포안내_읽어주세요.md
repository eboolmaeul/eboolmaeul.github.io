# 홈페이지 배포 안내 (GitHub Pages + 가비아 도메인)

도메인: **eboolmaeul.or.kr** (가비아)
호스팅: **GitHub Pages** (무료, 트래픽 제한 사실상 없음)
비용: 도메인 갱신비만 발생. 호스팅·SSL 인증서는 0원.

---

## 이 폴더에 들어 있는 파일

| 파일 | 역할 | 수정 여부 |
|---|---|---|
| `index.html` | 홈페이지 본체. 이 파일 하나가 전체 사이트입니다. | 내용 수정은 여기서 |
| `404.html` | 잘못된 주소로 들어와도 홈이 뜨게 하는 예비 파일 | 그대로 두기 |
| `CNAME` | 연결할 도메인 이름. `eboolmaeul.or.kr` 한 줄. | 그대로 두기 |
| `.nojekyll` | GitHub의 자동 변환 기능을 끄는 빈 파일 | 그대로 두기 |
| `robots.txt` | 검색엔진 수집 허용 설정 | 그대로 두기 |
| `sitemap.xml` | 검색엔진용 주소 목록 | 그대로 두기 |
| `logo-symbol.png` | 로고 심볼 (파비콘·블로그·SNS용) | 필요할 때 사용 |
| `logo-full.png` | 로고 전체 (심볼+글자) | 필요할 때 사용 |

> `.nojekyll`과 `CNAME`은 이름 그대로 두어야 합니다. 확장자를 붙이면 동작하지 않습니다.

---

## 1단계. GitHub 계정과 저장소 만들기

1. https://github.com 에서 회원가입 (무료). 계정 이름은 `eboolmaeul` 같은 조합 이름을 권합니다.
2. 로그인 후 오른쪽 위 **＋ → New repository** 클릭
3. 아래와 같이 입력
   - **Repository name**: `eboolmaeul.github.io`
     (계정 이름이 `eboolmaeul`이면 이 이름으로 만드세요. 계정 이름과 앞부분이 같아야 합니다.)
   - **Public** 선택 (Private은 무료 Pages가 안 됩니다)
   - Add a README 체크는 해제
4. **Create repository**

## 2단계. 파일 올리기

1. 만든 저장소 화면에서 **uploading an existing file** 링크 클릭
   (이미 파일이 있으면 **Add file → Upload files**)
2. 이 폴더의 **모든 파일**을 창으로 끌어다 놓습니다.
   - `.nojekyll`은 점으로 시작해서 탐색기에서 안 보일 수 있습니다.
     Windows는 보기 → 숨긴 항목 체크, Mac은 `Command + Shift + .` 를 누르면 보입니다.
3. 아래 **Commit changes** 버튼 클릭

## 3단계. GitHub Pages 켜기

1. 저장소 상단 **Settings** → 왼쪽 메뉴 **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `main` / 폴더는 `/ (root)` → **Save**
4. 1~2분 뒤 새로고침하면 `https://eboolmaeul.github.io` 주소가 뜹니다. 눌러서 화면이 나오는지 먼저 확인하세요.

---

## 4단계. 가비아에서 DNS 설정

가비아 로그인 → **My가비아 → 서비스 관리 → 도메인** → `eboolmaeul.or.kr`의 **DNS 관리 → DNS 설정** → **레코드 수정**

아래 5개를 추가합니다.

| 타입 | 호스트 | 값 / 위치 | TTL |
|---|---|---|---|
| A | @ | `185.199.108.153` | 3600 |
| A | @ | `185.199.109.153` | 3600 |
| A | @ | `185.199.110.153` | 3600 |
| A | @ | `185.199.111.153` | 3600 |
| CNAME | www | `eboolmaeul.github.io.` | 3600 |

주의할 점

- A 레코드 4개는 **모두** 넣어야 합니다. 하나만 넣으면 접속이 불안정합니다.
- CNAME 값 끝의 **마침표(`.`)**를 빼먹지 마세요. 가비아는 마침표가 없으면 뒤에 도메인을 덧붙여 버립니다.
- 호스트 칸에 `@`를 못 넣는 화면이면 비워 두면 됩니다.
- 기존에 가비아 기본 파킹 페이지용 A 레코드가 있으면 **삭제**하고 위 값으로 바꿉니다.
- 입력 후 반드시 **확인 / 저장** 버튼을 눌러야 반영됩니다.

## 5단계. GitHub에 도메인 연결하고 HTTPS 켜기

1. 저장소 **Settings → Pages → Custom domain** 칸에 `eboolmaeul.or.kr` 입력 → **Save**
   (`CNAME` 파일을 이미 올렸다면 자동으로 채워져 있습니다.)
2. "DNS check in progress"가 뜨면 그대로 두고 기다립니다. 보통 10분~1시간, 늦으면 하루까지 걸립니다.
3. 확인이 끝나면 아래 **Enforce HTTPS** 체크박스가 활성화됩니다. **반드시 체크하세요.**
   무료 SSL 인증서(Let's Encrypt)가 자동 발급되어 `https://eboolmaeul.or.kr` 로 접속됩니다.

> "Domain does not resolve to the GitHub Pages server"가 계속 뜨면 DNS가 아직 퍼지는 중입니다.
> 30분쯤 뒤 Custom domain 칸을 비웠다가 다시 입력하면 재검사가 돌아갑니다.

---

## 앞으로 내용 수정하는 법

1. GitHub 저장소에서 `index.html` 클릭 → 오른쪽 위 **연필 아이콘**
2. 내용 수정 후 아래 **Commit changes**
3. 1~2분 뒤 홈페이지에 반영됩니다.

자주 고칠 값은 파일 안 `const SITE = {` 부분에 모여 있습니다.
새 공지는 그 아래 `NOTICES` 배열 맨 위에 한 줄 추가하면 됩니다.

공지사항은 네이버 블로그(https://blog.naver.com/eboolmaeul)에만 올리시면 됩니다.
홈페이지는 블로그로 연결만 하므로 글을 올릴 때마다 홈페이지를 고칠 필요가 없습니다.

### 구글 폼 연결
1. forms.google.com 에서 양식을 만듭니다.
   항목: 문의 유형(객관식) / 성함(필수) / 연락처(필수) / 이메일(선택) / 문의 내용(필수) / 개인정보 수집·이용 동의(필수)
2. 오른쪽 위 「보내기」 → **&lt;&gt; 아이콘** → 나오는 코드의 `src="..."` 안쪽 주소를 `form` 에 넣습니다.
3. 같은 화면의 **링크 아이콘**에서 나오는 짧은 주소를 `formLink` 에 넣습니다.
4. 폼의 「응답」 탭에서 스프레드시트를 연결하고, 새 응답 이메일 알림을 켜 두세요.

수정 전에 파일을 한 번 내려받아 백업해 두세요. GitHub은 이전 버전을 모두 보관하지만,
익숙해지기 전까지는 손에 사본을 두는 편이 마음이 편합니다.

---

## 마지막으로 해두면 좋은 것

- **네이버 서치어드바이저**(searchadvisor.naver.com)에 사이트 등록
  → 사이트 소유 확인 후 `sitemap.xml` 제출. 네이버 검색에 노출됩니다.
- **구글 서치콘솔**(search.google.com/search-console)에도 같은 방식으로 등록
- 블로그 프로필과 스마트스토어에 홈페이지 주소 `eboolmaeul.or.kr` 기재
- 명함, 현수막, 공모사업 신청서에도 같은 주소 사용

---

## 아직 실제 값으로 바꿔야 하는 항목

`index.html` 안 `const SITE = {` 부분에서 아래를 채워 주세요.

- `form` : 구글 폼 **퍼가기(embed)** 주소 → 문의 페이지에 신청서가 표시됩니다
- `formLink` : 구글 폼 **일반 공유** 주소 → 「새 창에서 문의하기」 버튼에 연결
- `booking` : 네이버예약 주소 — **개설 후 입력**. 비어 있으면 「준비 중 — 전화 051-865-5152」로 표시됩니다.
- `store` : 스마트스토어 주소 — **개설 후 입력**. 비어 있으면 구매 버튼이 「주문 문의」로 바뀝니다.
- `bank` : 후원 계좌번호 — **개설 후 입력**. 비어 있으면 「계좌 개설 중」 안내가 나옵니다.

전화번호, 유튜브 채널(이불마을 TV), 버스 노선, 마을상품 가격, 체험 요금, 대관료는 이미 반영되어 있습니다.

그 밖에 활동 사진, 유튜브 영상 ID, 정류장에서 센터까지 도보 소요 시간,
연혁 세부 일자는 실제 자료로 채워야 합니다.
